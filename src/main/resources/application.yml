server:
  port: 8081  # Runs on port 8081

spring:
  application:
    name: user-service  # Service name (must match Gateway config)

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  # Eureka Server URL
